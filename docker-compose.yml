version: "3.9"

services:

  semantic-search-api:
    build: .
    container_name: semantic-search-api

    ports:
      - "8000:8000"

    volumes:
      - ./data:/app/data
      - ./chroma_db:/app/chroma_db

    restart: unless-stopped